stages:
  - build
  - deploy

build ubuntu-trusty:
  stage: build
  script: ./travis_docker.sh build
  variables:
    IMAGE: ubuntu-trusty

build ubuntu-touch-xenial:
  stage: build
  script: ./travis_docker.sh build
  variables:
    IMAGE: ubuntu-touch-xenial

build neon-bionic:
  stage: build
  script: ./travis_docker.sh build
  variables:
    IMAGE: neon-bionic

build debian-unstable:
  stage: build
  script: ./travis_docker.sh build
  variables:
    IMAGE: ubuntu-unstable


push ubuntu-trusty:
  stage: deploy
  script: ./travis_docker.sh push
  variables:
    IMAGE: ubuntu-trusty

push ubuntu-touch-xenial:
  stage: deploy
  script: ./travis_docker.sh push
  variables:
    IMAGE: ubuntu-touch-xenial

push neon-bionic:
  stage: deploy
  script: ./travis_docker.sh push
  variables:
    IMAGE: neon-bionic

push debian-unstable:
  stage: deploy
  script: ./travis_docker.sh push
  variables:
    IMAGE: ubuntu-unstable
